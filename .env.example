# =============================================================================
# Шаблоны окружения — выберите один:
#
#   Локально:  copy .env.local.example .env   +  .\run-local.ps1
#   Сервер:    copy .env.prod.example .env    +  deploy/docker compose
#
# Подробнее: docs/AI_ASSISTANT.md
# =============================================================================

# --- Локальная разработка (.env.local.example) ---
# SQLite, без Redis, Celery worker не нужен

# --- Сервер (.env.prod.example) ---
# PostgreSQL + Redis + Celery worker обязательны
